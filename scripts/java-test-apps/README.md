# Introduction
This directory holds simple java tests application that are run after a base-image is constructed. This test should prove that java configured is working as expected.

# Build
If you want to re-build java files or create new ones you can run:
```bash
javac jenvtest.java
```
# Run
If you want to run class files you do so by:
```bash
java jenvtest.java
```